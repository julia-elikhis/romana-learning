apiVersion: v1
kind: Service
metadata:
  name: {{ include "romanian.name" . }}
  labels:
    {{- include "romanian.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "romanian.selector" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
