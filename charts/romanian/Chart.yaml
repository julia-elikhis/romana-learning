apiVersion: v2
name: romanian
description: Romanian practice app using existing compute, PostgreSQL, and configurable course storage.
type: application
version: 0.5.0
appVersion: "0.3.0"
dependencies:
  - name: minio
    version: 5.4.0
    repository: https://charts.min.io/
    condition: minio.enabled
